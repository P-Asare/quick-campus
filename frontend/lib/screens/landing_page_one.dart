import 'package:flutter/material.dart';

class FirstLanding extends StatelessWidget {
  const FirstLanding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("landing one"),
      ),
    );
  }
}